         }
         fprintf(fp, "\n");
     }
    G_free(raster);
 
     return (0);
 }
         if (colcnt > 0)
             fprintf(fp, "\n");
     }
    G_free(raster);
 
     return (0);
 }
             fprintf(fp, "\n");
         fprintf(fp, "\n");
     }
    G_free(raster);
 
     return (0);
 }