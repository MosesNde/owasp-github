         }
     }
     G_free(astar_pts);
    G_free(contour);
    G_free(dist_to_nbr);
 
     return 0;
 }
 
     G_free(dist_to_nbr);
     G_free(weight);
    G_free(contour);
 
     return 0;
 }