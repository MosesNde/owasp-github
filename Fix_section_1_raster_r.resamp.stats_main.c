 
         Rast_put_d_row(outfile, outbuf);
     }
    G_free(row_map);
    G_free(col_map);
    G_free(values);
 }
 
 static void resamp_weighted(void)
 
         Rast_put_d_row(outfile, outbuf);
     }
    G_free(row_map);
    G_free(col_map);
    G_free(values);
 }
 
 int main(int argc, char *argv[])