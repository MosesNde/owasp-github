         }
         rowcount++;
     }
    G_free(raster);
     return;
 }
 
             fprintf(fp, "\n");
         }
     }
    G_free(raster);
     return;
 }
 
         }
         fprintf(fp, "\n");
     }
    G_free(raster);
     return;
 }
 
             }
         }
     }
    G_free(redraster);
    G_free(greenraster);
    G_free(blueraster);
     return;
 }
 
             fprintf(fp, "%.*f %.*f %.*f \n", dp, x, dp, y, dp, z);
         }
     }
    G_free(xraster);
    G_free(yraster);
    G_free(zraster);
     return;
 }