 
     /* Close file */
     Rast_close(map);
    G_free(rast);
 
     return data;
 }
 
     /* Close file */
     Rast_close(map);
    G_free(rast);
    G_free(frast);
    G_free(drast);
 }
 
 /* ******************** 3D ARRAY FUNCTIONS *********************** */