     free(tgt->username);
     free(tgt->groupname);
     free(tgt);
    crinitGlobOptDestroy();
 }
 
 void crinitTaskCreateFromConfKvListTestUserNumericSuccess(void **state) {
     free(tgt->username);
     free(tgt->groupname);
     free(tgt);
    crinitGlobOptDestroy();
 }